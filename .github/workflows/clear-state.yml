# .github/workflows/clear-state.yml

name: Clear Ghost Status Checks

on:
  pull_request:
    branches: [develop]

# 👇 가장 중요한 부분입니다.
# 명시적으로 쓰기 권한을 부여하여 GitHub의 내부 상태를 바로잡습니다.
permissions:
  contents: write

jobs:
  dummy-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run a dummy step
        run: echo "This job is to clear stale GitHub Action status checks."
